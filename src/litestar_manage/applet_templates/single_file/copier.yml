app_name:
    type: str
    help: Name of the new Litestar applet

init_templates:
    type: bool
    default: no
    help: Create a templates directory?

init_static_files:
    type: bool
    default: no
    help: Create a static files directory?

_envops:
    keep_trailing_newline: true
    trim_blocks: true
    lstrip_blocks: true